services:
  - type: web
    name: ace-backend
    env: python
    buildCommand: pip install -r requirements.txt
    # Bind to Render's provided PORT, otherwise health checks hang
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 600 --workers 2 --threads 2
