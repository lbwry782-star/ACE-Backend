services:
  - type: web
    name: ace-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 600 --bind 0.0.0.0:$PORT
    envVars:
      - key: PORT
        value: 10000
