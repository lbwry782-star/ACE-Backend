services:
  - type: web
    name: ace-backend
    env: python
    buildCommand: pip install -r requirements.txt
    # multiple workers are now safe because jobs are stored on disk
    startCommand: gunicorn app:app --timeout 600 --workers 2 --threads 2
